// repare poly / mega when low on health
// link it to point1 which repare every bot around


def unitHealthPercent()
    sensor health @unit @health
    sensor healthMax @unit @maxHealth
    op div healthPercent health healthMax
    return healthPercent


set p point1
sensor x p @x
senser y p @y

#ref resetCount
set c 0
op add c c 1

if c == 1
    ubind @poly
else if c == 2
    ubind @mega

else
    jump resetCount


hper = unitHealthPercent()

if hper <= 0.75
    ucontrol move x y 0 0 0
